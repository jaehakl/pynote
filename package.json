{
  "name": "pynote",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter pynote-note dev",
    "build": "pnpm --filter pynote-note build",
    "start": "pnpm --filter pynote-note start"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@rsuite/icons": "^1.3.2"
  }
}